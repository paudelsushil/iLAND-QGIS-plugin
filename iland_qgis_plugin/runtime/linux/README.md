# Linux Runtime

Expected executable path for auto-detection:
- `runtime/linux/iLANDc` (preferred)
- `runtime/linux/ilandc`

Included source snapshot:
- `runtime/linux/src/ilandc`

Notes:
- The source snapshot comes from `iland-model-main/src/ilandc` and is not a runnable runtime by itself.
- Place a native Linux `iLANDc` binary in this folder for the plugin to run simulations.
